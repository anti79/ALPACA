#!/usr/bin/env python

from os.path import realpath, dirname, join
import sys

sys.path.append(join(dirname(realpath(sys.argv[0])), '..', 'src'))

from alpaca import main


if __name__ == '__main__':
    main.main(sys.argv)
